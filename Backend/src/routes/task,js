const express=require("express");
const taskRouter=express.Router;


// post projects/:projectID/tasks - create tasks ------  only admins


// GET /projects/:projectId/tasks → Get all tasks of a project 



// GET /tasks/:id → Get details of a specific task



// PUT /tasks/:id → Update task (title, description, status, priority, deadline, assignedTo) ---  only admins



// DELETE /tasks/:id → Delete a task -- only admins




module.exports={
    taskRouter
}